--- /Hengly/apps/chartdb2/ui/src/pages/login-page/login-page.tsx
+++ /Hengly/apps/chartdb2/ui/src/pages/login-page/login-page.tsx
@@ -2,3 +2,3 @@
 import { useNavigate, Link } from 'react-router-dom';
-import { Mail, Lock } from 'lucide-react';
+import { Mail, Eye, EyeOff } from 'lucide-react';
 import { AuthLayout } from '@/components/auth-layout/auth-layout';
@@ -10,2 +10,3 @@
     const [password, setPassword] = useState('');
+    const [showPassword, setShowPassword] = useState(false);
     const [error, setError] = useState<string | null>(null);
@@ -77,3 +78,3 @@
                             <input
-                                type="password"
+                                type={showPassword ? 'text' : 'password'}
                                 required
@@ -84,4 +85,9 @@
                             />
-                            <div className="pointer-events-none absolute inset-y-0 right-0 flex items-center pr-1">
-                                <Lock className="size-5 text-blue-600 dark:text-blue-400" />
-                            </div>
+                            <button 
+                                type="button"
+                                onClick={() => setShowPassword(!showPassword)}
+                                className="absolute inset-y-0 right-0 flex items-center pr-1 text-blue-600 dark:text-blue-400 hover:text-blue-700 dark:hover:text-blue-300 focus:outline-none"
+                            >
+                                {showPassword ? <EyeOff className="size-5" /> : <Eye className="size-5" />}
+                            </button>
                         </div>
